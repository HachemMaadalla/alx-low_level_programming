#include <stdio.h>
#include "main.h"
/**
* puts2 - prints every other character of a string
* @str : char str
*/

void puts2(char *str)
{
for (int i = 0; i < strlen(s); i += 2)
{
_putchar(s[i]);
}
_putchar('\n');
}
