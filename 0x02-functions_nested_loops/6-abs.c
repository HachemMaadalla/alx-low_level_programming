#include "main.h"

/**
 * _abs - absolute value of an integer.
 * @n: integer to be chekced.
 *
 * Return: absolute value of the integer.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
