#include "main.h"

/**
 * print_diagonal - draws a diagonal line 
 * @n: length
 */
void print_diagonal(int n)
{
int l, s;
if (n > 0)
{
	for (l = 0; l < n; l++)
	{
		for (s = 0; s < ln; s++)
		{
			_putchar(' ');
		}
	_putchar('\');
	_putchar('\n');
	}
}
_putchar('\n');
}
